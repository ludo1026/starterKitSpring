/*
 * Created on 22 nov. 2013 ( Time 16:27:37 )
 * Generated by Telosys Tools Generator ( version 2.0.6 )
 */
package org.telosys.starterkits.test.persistence;


import org.telosys.starterkits.bean.Book ;
import org.telosys.starterkits.mock.BookMock;
import org.telosys.starterkits.persistence.PersistenceServiceProvider;
import org.telosys.starterkits.persistence.services.BookPersistence;

import org.junit.Assert;
import org.junit.Test;

/**
 * JUnit test case for Book persistence service
 * 
 * @author Telosys Tools Generator
 *
 */
public class BookPersistenceTest 
{
	@Test
	public void test1() {
		
		System.out.println("Test Book persistence : delete + load ..." );
		
		BookPersistence service = PersistenceServiceProvider.getService(BookPersistence.class);
		
		BookMock mock = new BookMock();
		
		// TODO : set primary key values here 
		process( service, mock, 0  );
		// process( service, mock, ... );
	}

	private void process(BookPersistence service, BookMock mock, int id ) {
		System.out.println("----- "  );
		System.out.println(" . load : " );
		Book entity = service.load( id );
		if ( entity != null ) {
			// Found 
			System.out.println("   FOUND : " + entity );
			
			// Save (update) with the same values to avoid database integrity errors  
			System.out.println(" . save : " + entity );
			service.save(entity);
			System.out.println("   saved : " + entity );
		}
		else {
			// Not found 
			System.out.println("   NOT FOUND" );
			// Create a new instance 
			entity = mock.createInstance( id ) ;
			Assert.assertNotNull(entity);

			// This entity references the following entities : 
			// . Author
			// . Publisher
			/* Insert only if references are OK
			// Try to insert the new instance
			System.out.println(" . insert : " + entity );
			service.insert(entity);
			System.out.println("   inserted : " + entity );
			*/

			System.out.println(" . delete : " );
			boolean deleted = service.delete( id );
			System.out.println("   deleted = " + deleted);
		}		
	}
}
